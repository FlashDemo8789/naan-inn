# Naan Inn

React + Vite site for `naan.jp`.

## Development
- `npm run dev`: start dev server
- `npm run build`: build for production

## Deployment
GitHub Pages deploys via `.github/workflows/deploy.yml` on pushes to `main`.
